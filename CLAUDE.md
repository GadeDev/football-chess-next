# CLAUDE.md — Football Chess Web版プロトタイプ 開発ガイド

Claude Code がこのリポジトリで作業する際の指針。詳細な背景は `HANDOFF.md` を参照。

## プロジェクト概要
- Unity製「Football Chess」（サッカー×将棋の同時ターン制対戦）を Web/PWA へ移植中。
- 現行のプレイ可能プロトタイプは単一ファイル `football-chess-prototype.html`（画像はbase64埋め込み、外部依存なし）。ダブルクリックで動作、スマホ対応。
- 対人対戦化はCloudflare Workers + Durable Objectsで追加中。`src/index.ts` がUniversoFutbol配下の対戦ルームAPI、`src/game-core.ts` がサーバー権威化に向けた純粋ゲームロジック抽出先。
- **現在の正しい作業対象はこのフォルダ**（`/Users/yanagiho-mba/football-chess-next/football-chess-repo/`）。`football-chess-next-clone` は別系統なので、このプロトタイプ修正では触らない。
- 方針：広告なし・サブスク課金・スマホ前提。**「面白く楽しめればよい、細部はおまかせ」**。グラフィック再現を重視。
- 正典＝Unity版実装。Unityソースは `/Users/yanagiho-mba/football-chess-unity-source/`（`Assets/_Chess/Scripts/`）。数値が食い違う場合は基本Unity実装を優先。CellDef準拠で1マス最大3駒（**2026-07-10修正：仕込み時チェックだけでは同時解決の収束で4体以上入ってしまうため、ローカルHTMLも実行時に満員再検証**`cellHasRoomFor`/`execPlainMovesTogether`の仮想位置判定**を行い中止する。サーバー`isMovable`再検証と同挙動・先着優先。表示`STACK_OFFSET`はUnity `ActorNormalOffsetMap`準拠で4=四隅/5=四隅＋中央まで対応＝オフサイド位置戻し等の過渡状態向け保険）。
- ユーザーは非エンジニア。**日本語で対応**。

## アーキテクチャ
- 現在は「単一HTMLのローカルプロトタイプ」＋「Cloudflare対人ルーム基盤」の二層構成。
- Cloudflare側の正式コンテンツパス想定は `/universofutbol/football-chess`、API prefix は `/api/universofutbol/football-chess`。
- `MatchRoom` Durable Object は1試合1インスタンス。青/赤/観戦者の席割り当て、WebSocket接続、コマンド受信、ルーム状態保存を担当。
- `src/game-core.ts` には盤面、初期配置、確率計算、seeded RNG、コマンド検証、サーバー側ターン解決の初期版を抽出済み。
- 青赤両方の `match.intent` が揃うと、`MatchRoom` は `resolveServerTurn` を呼び、移動/ドリブル/通常パス/スルーパス/こぼれ球/オフサイド/タックル/ファウル/PK/FK/CK/GK/シュート/得点後キックオフまでの結果イベントを `match.turn.resolved` で配信する。
- `football-chess-prototype.html` にはオンライン対戦バーを追加済み。`ROOM作成` / `参加` でWorkerへ接続し、オンライン中の `TURN END` はローカルAIではなく `match.intent` を送る。
- **オンライン視点変換（2026-07-10実装。Unity版準拠）**：**ホストもゲストも「自分=青駒・下から上へ攻める」見た目**。赤席には盤を点対称（`toViewXY`: x→-x, y→1-y。対合）で反転表示し、駒/軌跡/紙吹雪等の表示色も入れ替える（`viewTeam`）。**論理座標・チーム・通信・サーバーは一切不変**（表示とアニメ座標だけの変換。セルのクリックは論理座標のまま配線されるため入力系は無変換）。ファネルは①render()のセル配置②`cellCenterViewport`（全アニメ）③`renderSelectFan`④各所の駒画像/色クラス。ヘッダーは常に「あなた/あいて」表記（論理色の赤/青は出さない）。盤エリアが上下対称であることが前提（崩す場合は要再設計）。
- **試合開始導線＝単一「対戦」ボタン（2026-07-10仕様変更。ユーザー指定）**：ホームのフッターは大きな「対戦」ボタン（`#ogMatchBtn`）だけにし、タップで`#modeOverlay`（対戦モード選択）を開く＝**オンライン対戦／COM対戦／フレンド対戦**の3択。旧・`ogKickoffBtn`（KICK OFF=COM対戦）/`ogOnlineBtn`（オンライン対戦）/ホーム編成下の`ogFriendBtn`（🤝フレンド対戦リンク）は**廃止済み（戻さないこと）**。COM対戦の開始は`startComMatch()`に一本化（テレメトリ`match.start{mode:'com'}`＋`resetBtn.click()`。マッチング15秒フォールバックと共用の漏斗）。
- **フレンド対戦UI（2026-07-10実装。footballchessmaniacs FriendMatchScreen仕様）**：「対戦」→モード選択の「フレンド対戦」→`#friendOverlay`＝「部屋を作る」（既存ROOM作成＋接続→**ルームIDを大きく表示＋IDをコピー/URLをコピー＋参加待ちスピナー**。相手が席に着くと`room.presence`の両席検知で自動的に対戦画面へ）／「部屋に入る」（ID入力→既存`connectOnlineRoom`。**接続前に`online.apiBase=defaultOnlineApiBase()`必須**）。共有URLは従来の`?room=`形式。
- **自動マッチング（2026-07-10実装。続編footballchessmaniacsのMatchmaking DOをプロトタイプ規模に簡略移植）**：「対戦」→モード選択の「オンライン対戦」→`#matchingOverlay`（スピナー＋経過秒＋キャンセル）→`POST /matchmaking/join` を2秒ポーリング→先着ペア成立で`createRoomCode`を払い出し→両者が既存`connectOnlineRoom`でROOM参加（席は接続の先着順）。**時間内にマッチングしなければCOM対戦へ自動フォールバック＝完全秘匿（2026-07-10仕様変更・ユーザー指定）**：切替は9〜15秒ランダム（`MM_COM_FALLBACK_MIN_MS`/`MAX_MS`。毎回15秒ちょうどだと機械的に見えるため）→「対戦相手が見つかりました：{名前}」の成立演出（`matching.foundTpl`＋`aiOpponentName()`の実在プレイヤー風名前プール・緑チェック・キャンセル非表示・約1.3秒）→`startComMatch()`。**COMへの切替であることはUI・トースト・ログ・モード説明のどこにも出さない**（「ユーザーが冷める」との指摘。明示文言を復活させないこと。スモークテストで明示文言の不存在を固定済み）。テレメトリは正直に`mode:'com'`（非表示情報）。旧・30秒「COMと対戦する」提案ボタン`mmComBtn`は**廃止済み（戻さないこと）**。サーバーは単一`Matchmaker` DO（`MATCHMAKER`バインディング・migration v3）＝待機Map＋結果Map（8秒ポーリング途絶で離脱、結果TTL60秒）。maniacsにあるレーティング帯±200→±400段階拡大・リージョンシャード・WebSocket化は将来課題。
- **ランキング（プレミアム限定。2026-07-10実装。ユーザー指定）**：
  - **サーバー**：`src/ranking.ts`（Elo簡略版・純粋関数：初期1000/K=32/下限0。node直テスト可能な分離モジュール）＋`AccountStore`に戦績カラム（`rating/rank_wins/rank_draws/rank_losses`。冪等ALTER TABLEで後付け）と`recordRankedResult()`/`rankingTop()`を追加。**記録はサーバー権威のみ**＝`MatchRoom`がフルタイム（`resolvePendingTurn`内で`advanceMatchClock`後にstatusがfinishedになった時。スコアから勝敗判定）と投了（`handleResign`）で`recordRankedResult`を呼ぶ。クライアントから戦績を書き込むAPIは無い（改ざん防止）。1試合1回（`snapshot.rankedRecorded`フラグ。再戦開始と`rebuildPregameGame`でリセット）。同一アカウント同士（2席とも同じID）は自作自演防止でスキップ。ゲスト席は記録対象外、相手がゲストなら相手レート=1000とみなす。
  - **席とアカウントの紐付け**：クライアントはWebSocket接続URLに`session=<Bearerトークン>`を添付（`onlineSocketUrl`。ログイン時のみ）→`MatchRoom.fetch`が`AccountStore.sessionUserId()`で解決し`PlayerSeat.accountId`へ保存（同一clientIdの再接続でトークン未添付なら既存紐付けを維持）。
  - **API**：`GET /ranking/top`（Bearer必須）。**閲覧も掲載もプレミアム（サブスク有効）会員のみ**（未ログイン=`reason:'login'`/未加入=`reason:'premium'`を返しクライアントがロック画面を出す）。一覧は`sub_active=1`かつ`sub_until`未失効かつ1試合以上プレイした会員をレート降順で最大100件＋自分の順位（`me`）。
  - **クライアント**：フッターに「ランキング」タブ（`#ogTabRanking`→`ogSelectTab('ranking')`→`#ogPageRanking`）。`ogRenderRanking()`＝未ログイン→🔒＋ログイン導線／非プレミアム→🔒「ランキングはプレミアム限定です」＋サブスクページ導線／プレミアム→自分の順位カード＋トップ100リスト＋注記。文言はL10N `ranking.*`。
  - **対象試合**：オンライン対戦（自動マッチング/フレンド対戦とも。サーバーは区別しない）のフルタイム/投了。COM対戦・観戦は対象外。
- オンラインバーは `URL` ボタンで `?room=...` 共有URLをコピーでき、そのURLを開くと自動参加する。`退出` / `投了` / `再戦` も実装済み。再戦はフルタイムまたは投了後、青赤両者が希望すると同じROOMでターン1から再開する。ブラウザは `localStorage` にオンライン用 `clientId` を保持するため、リロード後も同じ席へ復帰しやすい。
- オンラインバーには表示名入力、青/赤の席、観戦人数、自分の席、送信済みマークを出すロスター行がある。表示名は `localStorage` に保存し、接続時の `name` パラメータと接続後の `client.hello` でROOMに反映する。`room.presence` / `match.intent.received` で更新され、観戦者にも同じ席状況が見える。
- 観戦者はスナップショット/ターン解決後も `phase='replay'` の読み取り専用を維持し、TURN ENDボタンもdisabledにする。観戦者から `match.intent` は送らない。
- `MatchRoom` は片側が `match.intent` を送った時点で3分の入力期限を設定する。期限までに相手が送らなければ、未送信側は空コマンドとしてサーバーでターン解決する。
- `MatchRoom` は `cleanupAt` を持つ。作成後未接続/全員退出のROOMは30分後、終了済みROOMは全員退出後6時間で `room_state` / `room_events` を削除する。WebSocket接続がある間は掃除予約を消し、観戦者の退出/切断でも `room.presence` を再配信する。
- オンライン時もサーバー側で15ターンハーフ、seeded AT（現HTML準拠: 前半0〜1/後半1〜3）、ハーフタイム後半キックオフ、フルタイム終了を処理する。`turn.additionalTurns` がスナップショットに含まれ、ブラウザ側はAT/ハーフタイム/フルタイムのイベントをログとカットインで再生する。
- ブラウザ側は `match.turn.resolved` を受けて `TurnEvent` を再生し、最終盤面はサーバースナップショットで同期する。通常移動/ドリブルの駒移動、シュートゴール/ブロック/ミス/GKセーブ、パス/スルーパス/パス失敗、パスカット、こぼれ球確保、オフサイド、PK/FK、CK/GK、遅延行為/消極的戦術系の代表カットインとボール軌跡はオンライン再生にも反映済み。タックル成功/シュートブロック/GKセーブ後のボール保持はリプレイ中にも即時同期する。
- `MatchRoom` は `match.leave` / `match.resign` / `match.rematch.request` も受ける。明示退出は席と保留入力を解放、投了は即 `finished`、再戦は両者同意でサーバースナップショットを初期化する。

## 単一HTML内
- CSS+JS全部入り。ロジックは1つの `<script>` ブロック（末尾付近）。
- 状態は `state` オブジェクト：`turn/half/scoreSelf/scoreOpp/selected/mode/expanded/ballSelected/popup/atFirst/atSecond/matchOver/phase`。
  - `phase`: `'input'`（仕込み中）/ `'replay'`（再生中）/ `'ended'`（試合終了）。
- 重要グローバル：`myCommands`/`oppCommands`（予約操作）、`chainHolderId`（論理ボール保持駒）、`ball`（`{target:'piece'|'cell', pieceId, x, y}`）、`MAX_PER_CELL=3`、`REGULAR_TURNS=15`、`COLS`(5列)/`ROWS`(8行)、`BOARD`(エリア定義)。
- 盤座標：青(b)は -Y 方向（敵ゴール y=-3）へ攻める／赤(r)は +Y 方向（自ゴール y=4）へ。`.board` のCSSアスペクト比は画像実寸に合わせ `705/1143`。
- オンライン時は `localPlayerTeam()` が操作チームを返す。オフラインでは従来通り青固定、オンラインでは割り当てられた青/赤席を操作する。自分判定を新規追加する場合、`team==='b'` 固定に戻さないこと。
- ローカルHTMLを `127.0.0.1:5174` などから配信する場合、オンラインAPIは自動で `127.0.0.1:8787/api/universofutbol/football-chess` へ向く。Cloudflare本番配信では同一originのAPIを使う。

## 実装済み機能（このプロトタイプの現状）
- 盤面/エリア、デフォルト編成、Unity駒画像、ボール2状態、パス範囲24方向、確率テーブル一式。
- **同時ターン制**（仕込み→AI裏で計画→「TURN END（決定）」でマージ→リプレイ再生）。
- **シュート判定フロー**（`resolveShootCommand`）：①経路上の敵DFで確定ブロック ②PA/VAテーブルでDFブロック抽選（唯一の確率ゲート）③GK位置判定（コース差±1でキャッチ）。PK/FK/CK成功率は`T_PK/T_FK/T_CK`（[攻撃コスト][GKコスト]の5x5表。**S3配信マスタ正式値**。2026-07-10修正：旧値は同梱LotteryData由来でPK/FKが決まりすぎていた）。下部固定ボタンは廃止済み。ボール選択時の弧ポップアップ内に、シュート可能エリアにいる場合のみ「SHOOT」項目が現れ、そこから`tryShoot`を呼ぶ（`buildSelectItems`/`renderSelectFan`）。
- **こぼれ球共通処理**：`placeLooseBall`/`pickupLooseBall`（コスト最高で確保、最高コスト複数ならUnity `GetHighestCostModel()` 準拠で抽選。サーバー側はseeded RNG）。**クリア処理**（自陣PA/GAでの守備成功時は中盤方向へ1マス押し出し `clearPushCell`）。こぼれ球アイコン(`.ballfree`)は駒(`.piece`)と同じ「セル幅%＋max-width上限」方式でサイズ指定（固定pxではない。画面幅変化に連動）。
- **オフサイド（2026-07-09全面修正）**：判定はUnity `IsOffside` 準拠で **before=パサー位置 / after=着地位置**。確率は配信マスタ `DrawingOffsideMaster` 正式値（キー=ボール移動方向。前進でライン越え着地=100%／横=50%／**後方パスは常に0%**。-1=無効組合せは0扱い）。オフサイドライン（`offsideLineFor`）はUnity `GetOffsideLine` 準拠＝守備側後方から2番目のY＋**ハーフウェイクランプは守備側陣地内に留める向き（青守備=Math.max(line,0)／赤守備=Math.min(line,1)）**。成立後処理 `handleOffside` はUnity `OnOffsideAsync` の簡略版＝受け手を開始位置へ戻し**オフサイド地点に最も近い相手チーム駒がボールを獲得**（`nearestOffsideKicker`。間接FK相当。サーバー側は `details.kickerId` をイベントに載せる）。HTML/`src/game-core.ts` 両実装とも同一修正済み・回帰テストあり。**旧実装の3バグを戻さないこと**：①クランプがb/r逆でライン=0に固定され通常パスまで100%誤判定、②before=after=受け手で後方パスも誤判定、③「成立地点にこぼれ球」は受け手が同ターン末に拾い直してオフサイドが実質無効化（カットインだけ出て進行が変わらない）。
- **Unity Calculateフェーズ寄せ**：`PrepareMoveOperations`準拠で「着地パス受け手の通常移動→非移動(パス/スルーパス/ドリブル/シュート)→その他の通常移動」にソート。`mIsMoveBall`相当の `turnBallMoved` で、ドリブル接触時とボール未移動ターン末のタックルを通常処理に統合。成功パス/スルーパス/シュートなどでボールが動いたターンは、着地先に相手駒がいてもターン末の静止タックルを行わない。Unity `IsChangedHasBallTeam()` 準拠で、ターン開始時からボール保持チームが変わった後のパス/スルーパス/シュートは後続コマンドでも中止する一方、ドリブルはUnity同様に駒移動だけは処理し、敵保持者のマスへ入った場合はタックル判定へ進む。フリーボール確保時は `lastHadBallTeam` を見て、同チーム回収ならターン開始位置ベースのオフサイドも判定する。オンライン入力検証もHTML/Unityの仕込み連鎖に合わせ、`chainHolder`/`BallModel.MoveNum`/`CanMove`相当と同ターン内の仮想コマ位置をUnity解決順で追跡して2本目以降のパスや移動済み受け手へのLandingPassを許可し、敵同居で停止した後の追加キックは拒否する。
- **FOUL/パス確率**：FOUL演出/PK/FKはUnity `TackleAsync` 準拠で、ボール保持チームから見た攻撃側2列（GA/PA/VA/Cross）のみ。中盤などではファウル抽選に当たっても通常タックルへ落ちる。FOUL発生率はUnity配信マスタ `DrawingFoulProbabilityMaster` の正式値（守備同マス1人=10%、2人=35%、3人=60%）。PASS確率表示はUnity `MoveRangeProvider.CalculatePassSuccessProbability` 準拠で、味方がいるマスのみ `経路上FlyingPass成功率の積 × 着地LandingPass成功率` を表示。空マス/相手だけのマスはスルーパス候補なのでPASS確率テキストを出さない。攻撃先GAに相手GKがいる場合はUnityのGK分岐に合わせ、通常パス/スルーパスともGKが100%カットして直接保持する。
- **ゴール後リキックオフ**：Unity `ReKickoffSetup` / `GetReKickOffTeamType` 準拠で、得点後は失点側ボールのキックオフ配置へ戻す。PA/GAへドリブル到達しただけでは得点にせず、得点はシュート/PK/FKなどの解決からのみ発生させる。
- **遅延行為/消極的戦術**：遅延行為はUnity条件に合わせ、「ターン開始時にボールを持っていたチームが、ターン終了時も同じチームとして自陣保持している場合のみ」カウントする。**成立時は相手チームのボールでキックオフ再開**（Unity `OnBattleDelayAsync`→`GetReKickOffTeamType`=FlipTeamType準拠。2026-07-10修正：旧実装は時間稼ぎした側のボールで逆だった。HTML/`game-core.ts`両方修正・回帰テストあり）。カットインはUnity `cutin/battleDelay.png`（審判シルエット）＋ラベル「時間稼ぎ」。**名前だけでは意味が伝わらないイベントには `CUTIN_SUBLABEL` でプレート下に小さな説明行を添える**（2026-07-10追加。`showCutin`の`opts.sublabel`で動的指定も可能）。旧ラベル「PASSIVE」は「消極的戦術」へ変更済み。**消極的戦術の毎ターンカットインは廃止**（うるさいというユーザー指摘。新規検出時のログのみとし、実際にペナルティが効いたタックル/パスカットのカットインへ動的サブラベル「消極的戦術ペナルティ適用中（-20%）」で明示する。戻さないこと）。
- **タックル/パスカットの成否演出（2026-07-10実装）**：成功=「タックル成功！」「パスカット成功！」／失敗=「タックル失敗」「パスカット失敗」を必ずプレートで明示する。失敗系はUnityの成功系画像を流用し`.concede`（彩度落とし）で区別（`CUTIN.tackleFailed=CUTIN.tackle`等）。パスカット失敗＝**競り合いがあったパスの成立時のみ**表示（素通りパスには出さない。`contested`/`defensiveContact`判定）。オフライン/オンライン両対応。消極的戦術は「ボールが下2マス外、かつ対象チームの9コマ以上が下2マス」に合わせている。PassiveTactics補正はUnity配信マスタ `DrawingPassiveTacticsMaster` の正式値（PassCut/Tackleとも-20%）。
- **シュート失敗後のCK/GK**：通常シュート失敗後のCK/GKリトライはUnity現行 `ShootAsync` に合わせ、初回CK後の追加CK判定が過剰連鎖しないようにしている。通常シュート失敗でCKにならずGKがゴールエリアにいる場合は `saving`、GKがゴールエリア外（同マス守備など）の場合は `failedShoot` として扱い、後者とファウル由来PK/FK→GKはUnity `OnGK` 同様にそのターンの後続コマンドを停止する。
- **シュートブロック順/最高コスト抽選**：Unity `IsSuccessShootAvoidanceBlock` 準拠で、シュート元→ゴール手前までの `GetRoute(..., false, false)` を順に見て、GK以外の相手がいるマスは65%で回避抽選する。最初に回避失敗したマスのGK以外・最高コスト守備者がブロックし、最高コストが複数ならUnity `GetHighestCostModel()` と同じく抽選する（サーバー側はseeded RNG）。
- **15ターン制**：前半15＋AT(0〜1)、後半15＋AT(1〜3)。ChessClockはAT中「45+N」「90+N」表記。
- **ボール軌跡演出**：Unity版のTrailRenderer風に、パス/スルーパス/ドリブル/シュート/オンライン再生イベントで `animateBallFlight` を使う。オンラインでは `pass.cut` に `from` と `cutAt` を持たせ、経路上/着地マスのカット地点までの軌跡を再生し、Unity `OnPassCutAsync` 準拠でカットした駒が直接保持する。セットプレー由来の `shot.saved` / `shot.goal` には `details.kickLogs` / `details.kickSteps` / `details.finalKickKind` が入り、CKカットイン再生と最終キック種別判定に使う。
- **選択UI（Unity風＋独自調整）**：マスタップ→**弧状ポップアップ**で駒/ボールを選択（`renderSelectFan`/`#selectFan`、-60°〜+60°の弧）。駒1体のみは1タップで即選択。項目2つ以上は「タップ→弧→項目タップ（ポップ演出）→選択確定→範囲表示」。**選択確定後にのみ**移動/パス範囲をハイライト（大きな脈動円）。範囲外タップでキャンセル。ボールは常に独立項目（保持球は保持駒の隣、こぼれ球は独立）。
  - `onPieceClick`は、駒選択中に「有効な移動先/パス先として、相手駒や別の自駒が乗っているマス」をタップした場合、`onCellClick`と同じ判定（`isMovable`/`isPassable`）を先に行ってから通す。個別駒のクリックハンドラが`stopPropagation`でマスのクリックを奪うため、この処理がないと「相手駒のいるマスへ移動/パスできない」バグになる。
- **ボール保持バッジの描画規則（2026-07-10修正）**：仕込み中(input)のボールバッジは `chainHolderId`（連鎖プレビュー）優先、**未設定なら実際の保持駒**に出す（`render()`内の`hasBall`）。`chainHolderId` は自チーム保持時のみ設定されるため、「input中はchainHolderIdのみ参照」に戻すと**相手チーム保持のままターンが始まるとボール表示が消える**実バグが再発する（オフサイドの相手ボール再開で顕在化した）。
- **移動先/パス先ゴースト**：自分の予約コマンドの移動先/パス先に半透明プレビュー（`.ghost`、input時のみ・自分のみ）。既存駒より手前(z-index高め)・点線の丸枠＋点滅・セル中心から少しオフセットして描画し、既存駒と重なっても視認できるようにしている。
- 演出（カットイン）は、パス／ドリブルとも「経路上または着地/移動先マスに敵駒がいて、実際に競り合い・奪取判定が起こり得る場合」のみ発火（`hasDefensiveContact`）。敵が誰もいない「素通り」では出さない。
- **コマンド種別(type)の決定原則（重要・複数回の回帰の元凶）**：`move`/`dribble`/`pass`/`throughpass`のtypeは、常に「ユーザーがどの行動を選んだか」だけで決まる。仕込み側(`movePiece`)・実行側(`execCommand`)のどちらも、実行時のライブなボール保持状態(`ballHolder()`)を見て暗黙にtypeを上書きしてはならない。スルーパス（`doPassToCell`、スペースへのパス）は`movePiece`を一切経由しない別経路。過去に「ballHolder()でcarryを上書きする」修正を入れて別のバグを誘発したことがあるため、今後この手のライブ状態参照は要注意。
- **会員/サブスク（2026-07-06追加。詳細は `AUTH_UNIVERSOFUTBOL.md`）**：アカウント基盤は Durable Object `AccountStore`（`src/accounts.ts`、PBKDF2ハッシュ、Bearerセッション30日）。API は `/auth/register|login|logout|me|sso` と `/billing/subscribe|cancel`。サブスクは**デモ課金（実決済なし・30日失効）**で、Unity準拠で**駒コスト変更がプレミアム限定**（フォーメーション変更・保存スロットは無料）。クライアントはタイトル→未ログイン時ログインダイアログ（ゲスト続行可）、フッター4タブ目「サブスク」ページ、セッションは `localStorage['fcSession']`。将来のUniversoFutbol(WordPress)連携は `?sso=<HS256 JWT>` → `/auth/sso`（`SSO_SECRET` 未設定時は501）で、`subscribed` クレームがゲーム側サブスクを上書きする。
- **アウトゲーム動線の改修（2026-07-10。UniversoFutbol他コンテンツとの整合性）**：**タイトル画面と自動ログインダイアログは廃止**し、起動で直接ホームを表示（`ogBoot`→`ogShowHome`。タイトル画像は`#ogHome`背景に暗めのオーバーレイ付きで転用＝※要検討につき1ルールで差し戻し可能）。ログインは**ヘッダー右上の「ログイン」ボタン**（`#ogHeadLoginBtn`、ログイン済みは非表示）から任意で開く。ログインウィンドウには**閉じる×**（`.closeX`）と**「UniversoFutbolでログイン（Google対応）」**（`#ogUfLoginBtn`→ポータル`https://universo-futbol.com/api/auth/launch?to=...`へ遷移。Google等の外部認証はポータル側が処理し`#uf_sso`で戻る）を追加。**フッターの「編成」タブは削除**（ホーム＝編成画面のため。`ogSelectTab`はoptional chainingでガード済み）、サブスク/設定ページに**「← 戻る」**（`.ogBackBtn`）を設置。サブスク表記は**月額¥2,000（UniversoFutbol共通。現状デモ）**。フォーメーション切替は**FLIPアニメ**で駒が滑らかに移動（`ogRenderDeck`内、`data-pi`で追跡。基準transformのtranslate(-50%,-50%)と合成するのを崩さないこと）。
- **アウトゲーム（タイトル/ホーム/編成/設定。2026-07-06移植）**：起動時にタイトル(`#ogTitle`)→タップでホーム(`#ogHome`)→KICK OFFで試合。Unity `TitleMain`/`HomeMain`/`UIDeckEditDialog` 準拠。編成はコスト5段階(1〜3)・チーム上限16（Unity `TeamDef.MaxTeamCost`。**増加時のみ**超過を拒否）・1マス自チーム3体まで・フォーメーション6種＋保存スロット6。`TeamFormationNormalMaster` / `DefaultTeamMaster` はUnity配信 `MasterData/Master.txt` から正式値を確認済みで、6種の配置と初期コストはすべて合計16.0。デッキは `localStorage`（`fcPrimaryDeck` / `fcDeckSlot0-5` / `fcClubName`）に保存し、オフライン試合の青チームへ反映（`rebuildDefaultPieces` が `playerTeamDef()` を使用）。オンラインではWebSocket参加時に `deck` JSONを送り、`src/game-core.ts` の `validateTeamDefinition` で検証してサーバー権威の初期盤面・得点後キックオフ・再戦に反映する。素材は `outgame_assets.json`（base64ソース）→ `--og*` CSS変数（`<style id="ogVars">`）。`?room=` 付き共有URLはアウトゲームをスキップして従来どおり自動参加。試合画面のHOMEボタンでホームへ戻れる。
- **ランク（コスト）変更UIの改善（第5弾。2026-07-10実装）**：表向きの呼称を「ランク」に統一（内部データ構造・保存キー・上限値はコストのまま不変）。①`#ogCostPicker`をグレースケール一律の分かりにくい選択肢から、5枚とも通常色のカード（★アイコン＋コスト数値）へ再設計。現在値カードに「現在」バッジ、上部に「TEAM COST 現在/16.0」プレビュー、各カードにタップ前から増減と変更後合計を表示（`ogCostPreviewTotal(pi,level)`）。上限超過カードのみロックアイコン＋「コスト超過」表示にし、タップ可能なまま既存の警告トーストを維持。上限判定はプレビュー計算・実行時チェックとも`ogCostExceedsLimit(pi,level)`に一本化（判定式の二重実装なし）。②選択確定でトースト（`ogRankLabel`の★表記＋変更後TEAM COST）と、変更した駒への0.3sパルス（`.ogPulse`）、TEAM COST表示のパンチ演出を追加。③駒コストバッジ(`.ogCost`)を約1.3倍に拡大、駒タップの押下フィードバック(`:active`縮小)、編成盤下に「駒をタップでランク変更」の常設ヒントを追加。④サブスク未加入時は**駒タップで無言で課金誘導画面へ飛ばしていた**旧挙動をやめ、ダイアログ自体は開いて5枚とも施錠表示＋「ランク変更はプレミアム限定です／プレミアムについて」ボタンを出す形に変更（`ogIsPremium()`判定ロジックは不変）。文言はL10N（`ogCostTitleTpl`/`ogCostPreviewTpl`/`ogCostToastTpl`/`ogCostHint`等）経由、`tFmt()`ヘルパーで`{pos}`等のプレースホルダ置換。他画面の「コスト変更」表記（サブスクページ・ゲスト案内・`ogShowSubRequired`トースト等）も「ランク変更（コスト変更）」へ揃えた。**今回やらないこと（将来検討）**：ランクごとの能力差の数値表示（現状マスタに強さパラメータの実データがないため）、駒画像のランク別差し替え（Unity版はコストでモデルIDが変わるが素材整理待ち）。
- 移動プレビューの `.ghost` CSSは `.board .ghost` にスコープ済み（`.btn.ghost` ボタンとの衝突で下部ボタン列が崩れていたのを修正）。セレクタを戻さないこと。
- パス／スルーパス予約済みの駒（`chainPassedIds`で記録）は、同じターンに通常移動を追加できる。スペースへのスルーパス後にパスを出した駒を走らせても、`throughpass`を消さず`move`を追加する。ドリブルは「ボールを運ぶ」操作なので、同じ駒のパス/シュートとは排他。
  - **パスモード中のタップ優先順位（2026-07-10修正）**：自動連鎖（`continueChainFrom`）で受け手がパスモード選択された状態で「既にボールを出した駒（`chainPassedIds`かつ未移動）」をタップした場合、戻しパスではなく**その駒の選択（移動の仕込み）を優先**する（`preferSelectOverPassBack`。`onPieceClick`/`onCellClick`両経路）。直前のパサーは必ずパス範囲内に居るため、これが無いとタップが常に戻しパスに化けて「ボールを出した駒が移動できない」実バグになる（Unity版はパス後の自動パスモードが無いので起きない）。まだボールを出していない味方へのタップは従来どおり連鎖パス。

## 次の残作業
- **直近更新（2026-07-11 第14便 / Claude Code）**：**COM AIの攻撃力強化**（ユーザー指摘「無操作の相手にすらシュートに至らない」）。高速フルマッチシミュレータ（タイマー/rAF加速のVM実行）で実測したところ、5試合で計シュート1本・保持駒はVA(y=2)で足踏み＝**敵陣では安全な横パス回しのスコアがPA突入・シュートを上回る**のが原因だった。修正＝①シュートにエリアボーナス（PA/GA=+30、VA=+12）②パス/ドリブルの前進価値の`Math.max(0,·)`クランプを撤廃（横パス・後戻りの回し続けを減点）③PA/GA（ゴール距離2以下）への持ち込みボーナス+30（守備壁への突入を恐れすぎない）④パス→シュート連鎖の閾値45→30。修正後の実測＝5シード全てでシュート2〜3本・2〜3得点・PA(y=3)到達。回帰＝COM戦フルタイムE2Eに「無操作プレイヤー相手にシュート系ログ≥1」を追加。
- **直近更新（2026-07-10 第13便 / Claude Code）**：**明示的なCOM対戦の相手名は「COM」表示**（ユーザー指定＝オンライン対戦との差別化）。`comPersona.stealth`フラグで区別＝モード選択→COM対戦（予約なし`resetBtn`）は`stealth:false`→「COM」／オンラインマッチングからの秘匿フォールバック（`aiOpponentName()`予約経由）は`stealth:true`→ペルソナ名のまま（**COMと明示しない方針と両立**）。フォーメーション多様化（6種ペルソナ）は両方で維持。ヘッダー/ハーフタイム/リザルトすべて`headerTeamNames()`経由なので自動で追従。回帰テスト1件追加（ai-behavior 7件）。
- **直近更新（2026-07-10 第12便 / Claude Code）**：COM AI修正2件（ユーザー指摘）。①**自陣停滞の解消**＝プレイヤーが全く動かないとAI(赤)が自陣から出ず遅延行為（3ターン）で相手ボールになっていた→`aiPlanHolder`末尾で赤が自陣(y<=0)保持中は`battleDelayCounts.r`に比例した圧力（25+カウント×40）で「敵陣へ持ち出す手」を加点・停滞/後戻りを減点。②**SS1トップ（GK脇張り付き）対策**＝`aiDeepThreats()`（赤陣y<=-1の青駒をランク×14＋ゴール近さで採点）を新設し、**攻守両局面で常時マーク最大2体**（守備=Defence②'でマスに乗る/届かねば寄せる、攻撃=全員攻め上がりをやめ`guardIds`の見張りを残す）。回帰テスト2件追加（遅延寸前で必ず前進×3シード/GK脇SS駒へのマーク攻守両方）＝`tests/ai-behavior.test.mjs` 6件。
- **直近更新（2026-07-10 第11便 / Claude Code）**：**シュートの弾道を「鋭いライナー」に**（ユーザー指摘「今はパスと同じ」）。`animateBallFlight`の`kind==='shoot'`は専用プロファイル＝①距離比例170〜300ms（従来520msの約2倍速）②弧はほぼゼロ（`Math.min(6,dist*.02)`）③蹴った瞬間が最速の軽い減速イージング④サイズ膨張なし・軌跡セグメント高頻度(10ms)。**呼び出し側の旧duration/arc指定（520/58等）は関数内で意図的に無視**（各所に残っているが浮ついた旧値のため）。CSS`.ball-trail.shoot`は細く鋭いスピードライン（高さ9px・白へ抜けるグラデ・.28s）。パス/ドリブルの軌道は不変。
- **直近更新（2026-07-10 第10便 / Claude Code）**：①**ヘッダーを「あなた/あいて」→実名表示に**（`headerTeamNames()`＝自分=設定のプレイヤー名/相手=オンラインは`presence`の相手名/COMはペルソナ名/観戦は青席・赤席名。8文字短縮`shortOnlineName`。未取得時は従来表記でフォールバック）。②**COMペルソナ**＝`COM_PERSONAS`（6フォーメーションごとに1人。名前にCOM/AI/CPUを含めない=秘匿方針）。`resetBtn`クリックで抽選し（`comPersonaNext`予約があればそれを消費）、**COM(赤)の編成もペルソナのフォーメーション＋既定コスト（合計16.0）で組む**（`comTeamDef()`。旧・DEFAULT_TEAM固定から変更）。マッチングのCOMフォールバックは`aiOpponentName()`がペルソナを予約するため、成立演出の名前とヘッダー名が同一人物になる。③**プレイヤー名がリロードで消える実バグ修正**＝原因はセッション復元（`ogRestoreSession`→`ogApplyUser`）が毎回アカウント名でlocalStorageのプレイヤー名を上書きしていたこと。`ogApplyUser({syncName})`化し**明示ログイン/SSO時のみ**同期（復元時は上書きしない・戻さないこと）。加えて`ogSetName`は`input`イベントでも即時保存。スモークテスト3件で固定。①**「駒をタップでランク変更」ヒントはプレミアム加入者のみ表示**（`ogRenderDeck`で`ogIsPremium()`ガード。未加入者に見せない＝ユーザー指定。ログイン反映時の再描画のため`ogApplyUser`が`ogRenderDeck()`を呼ぶ）。②**TEAM COST表記の7言語化**（`TEAM_COST_LABELS`＝en:TEAM COST/ja:チームコスト/ko:팀 코스트/es:Coste del equipo/pt:Custo da equipe/de:Teamkosten/zh-CN:球队成本。ホーム合計表示は`ogTeamCostTpl`、ランク変更ウィンドウの`ogCostPreviewTpl`/`ogCostToastTpl`もラベル置換。`setLocale`→`ogRenderDeck`で言語切替に追従）。スモークテストで固定。
- **直近更新（2026-07-10 第8便 / Claude Code）**：Issue #1（v1.0残作業）のP0/P1を実装。
  - **ブラウザE2E基盤（P0）**：Playwright＋`wrangler dev`（miniflareがDO込みでローカル完全再現）。`npm run test:e2e`。構成は`playwright.config.mjs`（**ポート8787固定**＝HTML側`defaultOnlineApiBase`が同一オリジン/8787前提。スマホ相当390×844・直列実行）。テストは`tests/e2e/`＝①起動回帰（ホーム表示→対戦→モード選択→COM対戦→盤面＋TURN END操作可、**pageerror収集で未捕捉例外をテスト失敗にする**）②COM戦フルタイム（開始→ハーフタイム→フルタイム→リザルト→ホーム復帰。約2分）③オンライン実WebSocket 2クライアント（フレンド対戦：部屋作成/参加/両席入力/ターン解決/**リロード再接続**/投了（confirmはdialogハンドラで承認）/再戦/退出＋自動マッチングのペア成立と青赤席割り）④15秒COMフォールバック実測とキャンセル維持⑤チュートリアル初回案内。**注意：`#board`はホームの背後に常時存在するためPlaywrightの可視判定だけでは接続確認にならない**（`onlineReconnectRole('')`が'b'/'r'になるまで待つ）。オフライン戦のログキーは`halftimeSimple`/`matchResult`（`halftime`/`fulltime`はオンライン再生用）。Chromiumは実行環境プリインストール（`PLAYWRIGHT_BROWSERS_PATH`）の`chromium`シンボリックリンクをexecutablePathで使用。
  - **⚠実バグ修正（E2Eが検出）**：`#tutorialOverlay`のz-indexが1200でホーム（`#ogHome`=4500）の下に潜り、「遊び方」から開いても操作不能だった→**5500へ修正（戻さないこと）**。
  - **初回起動チュートリアル案内（P1）**：初回起動時だけ`#tutorialPrompt`（見る/あとで見る）を表示。`fc.tutorial.prompted`（案内済み）＋既存`fc.tutorial.seen`（視聴済み・`tutorialOpen`が保存）をlocalStorageに持ち、以後はホームの「遊び方」から任意再視聴。`ogBoot`の`ogShowHome()`直後に`maybeShowTutorialPrompt()`（`?room=`共有URL起動では出ない）。
  - 検証＝E2E 10件 / `npm test` 56件 / audit:i18n 0件 / 型チェック / dry-run 全pass。
- **直近更新（2026-07-10 第7便 / Claude Code）**：ユーザー指定の仕様変更3点。①**試合開始を単一「対戦」ボタンに統合**（`#ogMatchBtn`→`#modeOverlay`でオンライン/COM/フレンドの3択。旧KICK OFF・オンライン対戦ボタン・ホームのフレンド対戦リンクは廃止）。②**オンライン自動マッチングは15秒でCOM対戦へ自動フォールバック**（Unity版仕様。旧30秒COM提案ボタン廃止）。③**ランキングをプレミアム（課金）ユーザー限定で実装**（Elo簡略レート・サーバー権威記録・`GET /ranking/top`・フッター「ランキング」タブ。詳細は上記アーキテクチャ欄）。検証＝`npm test` 56件pass（ranking純関数テスト＋スモーク3件追加）・`npm run audit:i18n` 候補0件・Worker型チェック・`wrangler deploy --dry-run`・HTML `node --check`・DOMスタブでのブート/15秒フォールバック動作シミュレーションまで実施。**未デプロイ**（デプロイはユーザー許可後）。
- **⚠ 確率マスタの未照合差異（2026-07-10発見・要判断）**：Unityは`USE_BASE64_MASTER`有効（ProjectSettings確認済み）＝**S3配信 Master.txt が全Drawing*テーブルの正典**で、同梱`StreamingAssets/LotteryData/*.json`は使われていない。PK/FK/CKは配信正式値へ修正済みだが、**以下も配信版と食い違ったまま**：`T_TACKLE`（配信は全体的にフラット40〜55）／`T_LANDING_PASS`（配信40〜50）／`T_FLYING_PASS`（配信70〜80。Web値5〜35と大差＝成功率/カット率の意味も要確認）／`T_PA_SHOOT`（配信55〜65）／`T_VA_SHOOT`（配信15〜35）／`T_FAILED.PenaltyAreaShoot`（配信[75,65,55,45,35]）／`T_FAILED.VitalAreaShoot`（配信[50,45,40,35,30]）。これらはゲームバランス全体が変わるため、まとめて差し替えるかはユーザー判断待ち。行列の向きはUnity `DrawingRepository`のGet*Entryで確認できる（PK/FK/CK/Tackle=[攻撃][守備]、PA/VAシュート=[GK][攻撃]、Flying/LandingPass=[相手][蹴る/受ける]）。なおThroughPass/ShootCutは配信マスタにシート自体が無い（Unityも未使用/コメントアウト）。
- **直近更新（2026-07-10 第6便 / Claude Code）**：①**ゴール演出増強＋対戦中★ランクバッジ＋ハーフタイム幕間**＝`fba84ed`。②**タックル/パスカットの成否演出＋消極的戦術カットイン整理**（毎ターン表示廃止→適用時サブラベル）＝`e92aa8f`。③**COM AIを期待値ベースに全面強化**（本体の確率関数でスコア化・パス→シュート連鎖・スルーパス活用・突入タックル守備。フルマッチ自動進行で検証）＝`582769e`。④**オンライン自動マッチング**（maniacs Matchmaking簡略移植：Matchmaker DO・2秒ポーリング・30秒COM提案。migration v3）＝`9375805`。⑤**オンライン視点変換（自分=青・下から上。Unity版準拠）＋フレンド対戦UI（maniacs FriendMatchScreen仕様：ID/URLコピー・参加待ち・自動開始）**＝`293e14e`（2タブ実対戦で検証）。全件本番デプロイ・ハッシュ照合済み。`npm test` 38件pass。
- **直近更新（2026-07-10 第5便 / Claude Code）**：①**時間稼ぎ（遅延行為）のボール帰属を修正**＝`5acc19a`（遅延側ボールで再開していた→Unity準拠で相手ボールに。Unity `battleDelay.png`プレート＋ラベル「時間稼ぎ」も追加。回帰テスト有）。②**1マス4体以上の駒収束を修正**＝`b310722`（実行時の満員再検証`cellHasRoomFor`＋一斉移動の仮想位置判定。表示は`ActorNormalOffsetMap`準拠で5体まで対応）。③**「PASSIVE」→「消極的戦術」**＋`CUTIN_SUBLABEL`で効果説明行を追加＝`8580f32`。④**ターンエンド/ターンスタート帯**（Unity `turn_cutin.png`＋LocalizeMaster正式文言）＝`85ab91a`。⑤**アウトゲーム動線改修**＝`6066c45`（タイトル画面・自動ログイン廃止→直接ホーム、右上ログインボタン、ログイン×/UniversoFutbolログイン（Google対応・ポータル`/api/auth/launch`経由）、サブスク/設定「←戻る」、編成タブ削除、サブスク¥2,000表記、フォーメーションFLIP移動）。全件本番デプロイ・ハッシュ照合・実機検証済み。`npm test` 38件pass。
- **直近更新（2026-07-10 第4便 / Claude Code）**：**PK/FK/CK成功率をS3配信マスタの正式値へ修正**＝`a4b47f4`（「FOUL/FKが怪しい」報告の調査で、Unityが`USE_BASE64_MASTER`有効＝配信Master.txtを使用しており、Web版の移植元だった同梱LotteryData JSONは未使用と判明。FK 45〜85%→正式40〜70%など決まりすぎを是正。FOULフロー自体・発生率・FailedPK/FK/CK・キッカー選定は正典どおりで変更なし。HTML/`game-core.ts`両方修正、CK参照2テストの期待値更新で37件pass）。**他テーブルの配信差異（Tackle/Flying/LandingPass/PA・VAシュート等）は上記⚠残作業に記録・要判断**。※このコミットには並行Codexセッションの第5弾（ランク変更UI）のコード変更が同梱されている（同時編集の交錯によるもの。構文・デプロイ検証済み、記録は`c29caa0`）。
- **直近更新（2026-07-10 第3便 / Claude Code）**：①**通常移動の一斉再生**＝`7a0918f`（連続する'move'／ボール非運搬`piece.moved`をグループ化しPromise.allで同時アニメ。解決順・結果は不変。最大7駒同時を実機確認。「1駒ずつは遅い」というユーザー要望）。②**ゴール演出（第4弾）**＝`b4dfe27`（`playGoalCelebration`：一拍→GOALカットイン＋フラッシュ→スコアパンチ→収束の約2秒。自分の得点は派手に/失点は祝わない。全ゴール経路が単一漏斗で通過、`GOAL_FX`定数、タップスキップ、二重発火ガード。同期ズレなしを診断済み）。いずれも本番デプロイ・ハッシュ照合済み。`npm test` 37件pass。
- **直近更新（2026-07-10 第2便 / Claude Code）**：①**試合終了リザルト演出（第3弾）**を実装＝`35ebbd0`（4拍構成：FULL TIMEカットイン→勝敗ワード[勝ち=金WIN＋win_efect回転光/負け=LOSE/分け=DRAW]→スコア＋ログ復元の得点タイムライン→再戦/HOME。タップスキップ・オンライン再戦ハンドシェイク接続・相手の再戦希望注記。秒数は`RESULT_TIMING`）。②**パスを出した駒が移動できない問題**を修正＝`2ed9075`（自動連鎖のパスモード中、パサーへのタップが戻しパスに化けていた→`preferSelectOverPassBack`で選択優先）。③並行セッションが**別チェックアウトから旧版をデプロイし本番が巻き戻る事故**が発生→現リポジトリ最新から再デプロイで復旧、デプロイ欄に再発防止手順を追記＝`c2f35c3`。SSO連携`dfb9eb3`（Codex）もこのブランチに合流済み。`npm test` 37件pass。
- **直近更新（2026-07-09〜10 / Claude Code）**：スマホ実機の不具合報告を起点に6件を修正し、いずれも `mini.footballchess.io` へデプロイ済み。①TURN ENDが押せない問題（`#board`の透明ゴール行がフッターのタップを奪っていた→ヘッダー/フッター行に `z-index:20`）とターン入力タイマー実装（20秒、時間切れでTURN END自動発火。`syncTurnTimer()` 集約）＝`1075b71`。②フルタイム後の手詰まりに結果ダイアログ `#fulltimeResult`（もう一度プレイ/ホームへ）を追加＝`8ff64a9`。③オフサイドをUnity正典へ全面修正（ラインクランプのb/r取り違え・ボール移動方向を無視した表引き・こぼれ球の自己拾い直しによる無効化の3バグ。HTML/`game-core.ts`両方、回帰テスト3件追加で `npm test` 37件）＝`45fb7d4`。④相手チーム保持時にボール表示が消える描画バグ修正＝`b320250`。⑤カットインを全種中央帯・同一サイズに統一（駒追従の小型プレート廃止）＝`7f84c35`。⑥ローカルリプレイの駒移動をワープから滑走アニメへ（オンライン再生と同方式に統一）＝`873e5fa`。詳細な設計判断・「戻さないこと」制約は各セクション（UI方針/実装済み機能）に記載済み。
- **直近更新（2026-07-07 / Codex）**：`bc51970` でフリーボール回収オフサイドのターン開始位置判定を固定、`ebceee8` で消極的戦術の発生条件とPassCut/Tackle補正対象を固定。その後、オンライン再生用に `command.skipped` へサーバー基準の `from`/`target` を追加し、HTML再生は `event.from` を優先するようにした。続けて `shot.goal` / `shot.saved` / `tackle.*` もトップレベル `from` を持つよう正規化。さらに `looseball.picked` / `offside` に発生座標を追加し、`offside.details.resetTo` でオンライン再生時に対象駒が戻る演出を入れた。成功パス/スルーパスには `details.defensiveContact` を持たせ、オンラインでも敵接触がある成功パスだけPASS/THROUGHカットインを出す。セットプレー由来の `shot.goal` / `shot.saved` には `kickLogs` に加えて構造化 `kickSteps` と `finalKickKind` を載せ、直接シュート/PK/FKゴールも `source` から `finalKickKind` を補完する。オンライン/ローカル再生はUnity `OnStartCK()` と同じ `saving`→`ck` カットインを再生し、オンラインは `kickSteps` 優先でログにもCK/PK/FK/PAシュートなどの最終キック種別を表示する。ローカルHTMLもUnity `OnGK()` に合わせ、PK/FK/CK後のGKリスタートと通常シュートの `failedShoot` 相当では後続コマンドを止め、GKゴールエリア内の通常セーブだけ続行する。さらにUnity `DribbleAsync` 準拠で、同ターン先行パスカット後のドリブル操作も駒移動として解決し、オンラインログでも「ドリブル移動」と表示する。ボールなしドリブルが新しい敵保持者のマスへ入った際のタックル分岐も回帰テスト化済み。`npm test` は34ケース全pass、`npm run check:worker` とHTMLスクリプトの `node --check` もpass済み。現在のブランチは `codex/prototype-throughpass-move-fix`、remote は `GadeDev/football-chess-next`。
- **今回更新（2026-07-07 / Codex）**：Unity `StateBattleCalculate` の `OnPassCutAsync` / `OnSaving` / `OnFailedShoot` / `OnFoul` / `OnTackleSuccess` / `OnOffside` / `OnShootStopAsync` に合わせ、サーバー権威ロジックとローカルHTMLの防御イベント発生時に `battleDelayCounts` を即リセットするよう更新。`shot.saved` の `saving` / `failedShoot`、ファウルセットプレーGK後の既存テストへ遅延カウントリセット期待値を追加済み。`npm test` は34ケース全pass、`npm run check:worker`、HTMLスクリプト `node --check`、`git diff --check` もpass済み。
- **今回追加（2026-07-07 / Codex）**：ファウルPK/FKはUnity同様にファウルされた保持者ではなく、攻撃側の最高コスト駒をキッカーとして扱うことを回帰テストで固定。ローカルHTMLのファウルセットプレー結果にも `kicker` / `kickFrom` を持たせ、PK/FKゴール、PK/FK→GK、CK経由→GKのボール軌跡開始点をサーバー/オンライン再生と揃えた。CK経由でGKになった場合はゴール方向からGKへ戻る軌跡に変更。`npm test`、`npm run check:worker`、HTMLスクリプト `node --check`、`git diff --check` はpass済み。
- **今回追加（2026-07-07 / Codex）**：Unityクローン内の `UrlDef.MasterData` が指すS3配信 `MasterData/Master.txt` をbase64デコードして未同梱扱いだった正式マスタを確認。FOUL発生率を `DrawingFoulProbabilityMaster` 正式値（1人10%/2人35%/3人60%）へ更新し、`TeamFormationNormalMaster` / `DefaultTeamMaster` 正式値でアウトゲーム6フォーメーションと既定編成を合計16.0へ戻した。旧18.5デフォルトのサーバー検証互換例外は削除。`DrawingPassiveTacticsMaster` はPassCut/Tackleとも-20%で現行実装と一致。`npm test`、`npm run check:worker`、HTMLスクリプト `node --check`、`git diff --check` はpass済み。
- ~~オンライン対戦への編成反映~~ → 2026-07-07 に実装済み。ROOM参加時に `deck` を送り、サーバー側で11人/GK1人/自陣配置/1マス3体/コスト上限を検証する。Unity配信マスタ確認後、既存デフォルト編成も正式値（合計16.0）へ戻したためコスト超過の互換例外は不要。
- Unity版との差異をさらに潰す。優先は `StateBattleCalculate` 周辺の同時解決エッジケース、オンライン再生イベントの細部、配信マスタと手元実装の残り照合。
- サーバー権威化の回帰テストを増やす。`src/game-core.ts` に対して、HTML版/Unity版から拾った固定盤面・固定乱数のケースをテスト化する。2026-07-07時点で `npm test` に34ケース（スルーパス+通常移動、PASS表示、オンライン連鎖パス検証、移動済み受け手へのLandingPass検証、pass→move順のLandingPass検証、成功パス後のターン末静止タックル抑止、ドリブルが通常移動より先に解決されるUnity順、先行パスカット後のドリブル駒移動、ボールなしドリブルの敵保持者タックル、経路/着地/到達マスのパスカット直接保持、保持チーム変更後のキック系中止、`command.skipped` の再生座標メタデータ、GA上GKの100%パスカット、PA自動得点防止、得点後キックオフ/同ターン後続停止、通常シュート失敗時のsaving/failedShoot分岐と再生メタデータ、CK分岐/CKゴールを含む `kickSteps` / `finalKickKind` メタデータ、ファウルPKゴール/PK→GKターン停止、シュートブロック同コスト抽選、こぼれ球同コスト抽選、フリーボール回収オフサイドのターン開始位置/戻し先判定、消極的戦術の発生条件とPassCut/Tackle補正対象）を追加済み。
- オンライン再生の完全対応。現状は主要 `TurnEvent` を再生済みで、2026-07-07に得点/フルタイム時のサーバースコア即時反映、キックオフ/遅延/ハーフタイム/フルタイムのカットイン直前に最終スナップショットを背景盤面へ反映する処理、`tackle.failed`/`command.skipped`の小型カットイン、通常シュート開始の`SHOOT`カットイン、シュート失敗時のゴール方向軌跡とGK保持への戻り軌跡、CK後GK保持時のゴール方向からの戻り軌跡、オフサイド時の対象駒戻し演出、接触あり成功パスのPASS/THROUGHカットイン、`kickSteps` による `saving`→`ck` カットイン、`finalKickKind` による結果ログの詳細化も追加済み。ローカルHTML側のCK/GK再生とオンライン `kickLogs` フォールバックも、CK開始時だけ `saving`→`ck` を出し、CKキック開始ログではCKカットインを二重表示しない。`command.skipped` はサーバー基準の `from` を優先して表示し、対象地点 `target` もイベントに残す。`shot.goal` / `shot.saved` / `tackle.*` / `looseball.picked` / `offside` もトップレベル `from` を持つ。残りは全イベントをUnity風の正確な順序・間・カットイン・軌跡に寄せること。
- 本番運用準備の残り: 正式ルーティングは**universofutbol.comゾーンが別Cloudflareアカウントにあるため保留**（Worker側のベースパス配信は実装済み。詳細はAUTH_UNIVERSOFUTBOL.md）。観戦共有URL（`?role=spectator`）、切断時の5回自動再接続/再接続案内、WebSocket close code/reasonやROOM errorの詳細ログ表示は2026-07-07実装済み。レート制限とROOM作成制限は2026-07-07実装済み。**wrangler.jsoncの`workers_dev: true`は消さないこと**（routes追加時にworkers.devが自動無効化されて公開URLが落ちた事故あり）。
- ~~公開URLでのプロトタイプHTML配信~~ → 2026-07-06 に static assets 方式で実装済み（下記デプロイ欄参照）。
- PWA/スマホ仕上げ: **2026-07-07実装済み**: Webマニフェスト+アイコン(`pwa/`→`build:assets`が`public/`へコピー。ホーム画面追加でスタンドアロン起動)、リプレイ中の盤面タップ抑制(`body.replaying`)、狭い端末でのオンラインバー折り返し/長い表示名の省略表示、設定画面の効果音/触覚フィードバックON/OFFと軽量SE。Service Workerは意図的に未導入(HTML更新がキャッシュ固定される事故防止。オフライン対応する際はバージョン付きキャッシュで設計すること)。

## UI方針（2026-07-08確定）
- **演出優先・盤面最大化を最優先**。スマホ基準の1画面レイアウト（スクロールなし・TURN END常時可視）を全環境共通とする。ヘッダー=スコアのみ、フッター=時計｜残り時間バー｜TURN ENDの1行。
- **UnityHUD忠実再現（フェーズ1）は方針変更により中止**（2026-07-08）。棚卸しレポート（BattleHud.prefab、基準750×1334）は資料として有効。中断時にファイル変更は発生しておらずstashは無し（直前のスマホ1画面修正はコミット `31386a5` として保全済み）。
- **画面文言はL10N辞書経由**（スクリプト冒頭の `L10N` と `t(key)`。現状 `state.lang='ja'` 固定）。以後、文言の追加・変更は必ずこの辞書に載せる。
- **盤ジオメトリはUnity `CellDef` 準拠**（2026-07-08修正）：セル=1.28×1.65世界単位。`.boardBox`＝盤画像そのもの（705:1143）で、`#board`/`#selectFan`（5×8グリッド箱）は `left:4.61%/width:90.78%/top:-8.968%/height:115.486%`。ゴール行は画像の上8.97%・下6.52%へ透明のままはみ出し、ヘッダー/フッター領域に重なる（ゴールマスは `pointer-events:none`。元々タップ無効）。セル座標のJS計算（k/8, k/5）は `#board` 基準のままでよい。**画像全体を均等5×8で割る旧方式に戻さないこと**（駒とピッチ描線がズレる）。
- **レイアウトは密着上詰め**（2026-07-08確定）：ヘッダー直下に盤画像、直下にフッター（間隔0）。縦の余りはすべて画面最下部の無地ダーク背景へ。**額縁風グラデーション背景・上下センタリング配置は実機で不評につき禁止**（「フィールドに変な枠が入った」ように見える）。背景演出を試す場合はモック画像で事前確認を取ること。
- **金色シュートマーク**（2026-07-08）：Unityに専用画像はなく `ColorDef.ShootBall=Color.yellow` のボールティント方式。Webは既存ボール画像のCSS金色フィルタ＋明滅（`.shootMark`）で再現。表示条件は扇形SHOOT項目と同一の `calcShoot(p).area!=='-'` を**同じ関数呼び出しで参照**する（条件式の複製・改変禁止）。リプレイ中は非表示。
- **複数駒マスは重ね描き**（2026-07-08）：数字バッジ廃止。`STACK_OFFSET`（セル%基準）で先頭駒を手前・通常サイズ、2/3体目は `.stackBack`（85%縮小）で斜め後ろ。
- **スコアヘッダー素材**：三角錐は `score_assets.json` の切り出し（`--scoreTriB`/`--scoreTriR`、縦横比105:125維持）＋バー中央部（`--scoreBarMid`）。全幅ストレッチ一枚絵（`--scoreImg`、現在未使用）に戻すと三角錐が潰れるので戻さないこと。
- 削除済みUI（2026-07-08）：配置リセット/タックル実演ボタン、操作説明文（modeHint）、凡例、ヘッダーの「🌐 ONLINE」文字（アイコン化）、MATCH TIME円形ロゴ（テキスト時計に置換）。`#resetBtn` はKICK OFF処理が `click()` を呼ぶため**非表示で残置**（削除禁止）。HOME/オフサイド実演/ログは☰メニュー内へ移設。
- 旧版ChessClock枠デザイン（円形文字盤）の再現は将来の任意課題（`--clockDial`/`--clockFrame` のbase64アセットは残置済み）。
- **ターン入力タイマー（2026-07-09実装）**：フッターの残り時間バー（`#timerBar`/`#timerLabel`）は `INPUT_TIME_SEC=20` 秒のカウントダウン。Unity `BattleSequence.TurnElement` の `FCTime.Out(BattleDef.MoveTurnTimeSecond)` 相当で、時間切れは仕込み済みコマンドのまま `endTurnBtn.click()` を自動発火。オフラインAI戦の仕込み中のみ作動（オンライン=サーバー3分期限が権威／リプレイ・試合終了・アウトゲーム表示中は停止して満タン表示）。開始/停止は `syncTurnTimer()` に集約し、`render()` と `ogShow()` から呼ぶ（ホーム画面表示中に裏で自動決定しないため。この呼び出しを消さないこと）。
- **COM AI（2026-07-10第2次改修：ワンパターン解消＋盤面反応型守備）**：ユーザー指摘「フォーメーションの組み合わせでキックオフから毎回同じ手順でシュートまで行きワンパターン」への対策。
  - **行動選択の多様化**：旧「上位2択85/15」はスコア差が開くと毎試合同じ手になるため、`aiPickTop`を「各候補±5の評価ゆらぎ→ベストの82%以上（最大4候補）からスコア重み抽選」へ変更。良手の範囲内でだけ散らすので弱くならない。支援移動にも±3のゆらぎ。
  - **守備の全面改修（Defence分岐）**：旧「近い順に寄せるだけ」→盤面反応型の4段階＝①**シュートレーン封鎖**（保持者→自ゴール(0,-3)の`getRoute`経路＋ゴール前(0,±1,-2)の空きマスへ、届く駒のうち**高ランク優先**で配置。シュート圏内=2体/接近中（ゴールまで5マス以内）=1体）②突入タックル最大2体 ③**受け手マーク**＝保持者のパス範囲の青駒を「シュート期待＋ランク×12＋ゴールへの近さ」で採点し上位3体のマスへ乗る（着地パスカット＋ターン末タックル圏。SS級の受け手を最優先・高ランク守備を充てる）④残りは帰陣（ボールと自ゴールの中間へ。既にゴール側なら維持）。**注意**：Unity解決順で通常移動はシュートより後のため封鎖は「次ターンの先回り」。だから接近段階から発動する。
  - **攻撃も相手のランクを考慮**：`aiReachThreatAt`（次ターンに踏み込める青駒の最大タックル成功率）を新設し、パス受け手・ドリブル先・支援移動の評価から割り引く（SS級の目前に運ばない）。
  - **回帰テスト**：`tests/ai-behavior.test.mjs`（DOMスタブ＋シードRNGで決定論）＝同一キックオフ16回で4通り以上のプラン/シュート圏の保持者へレーン封鎖+突入≥2/守備反応数/リスク関数。VMヘルパーは`tests/helpers/prototype-vm.mjs`（`run()`でトップレベルlet/constに触れられる）。
- **COM AI（2026-07-10全面強化）**：`planOpponentAI`＋`aiPlanHolder`は**期待値ベース**。ゲーム本体と同じ確率関数（`calcShoot`/`calcPassDisplayProbability`/`calcFlyingPass`/`calcTackleSuccess`/`calcOffside`）で全行動をスコア化し、上位2択から85/15で選ぶ（それ以外の乱数なし）。①保持駒＝シュート/通常パス（オフサイド率・着地被タックル率で割引、実質成功率28%未満は捨てる）/スルーパス（前方空きマス限定・回収レースで赤が近い時だけ）/ドリブル（被タックル期待を減点）の定量比較。②**パス→シュートの連鎖仕込み**（受け手のシュート期待45%以上。パス失敗時は実行側の保持チェックで自動中止）。③非保持駒＝オンサイド維持（ライン越え-25点）＋1体がパスコース形成。④守備＝最大2体がホルダーのマスへ突入（ターン末タックル）＋最寄り4体で包囲＋中間駒がパス先候補をマーク。⑤こぼれ球＝近い順・同距離ならコスト高優先（拾得ルールと整合）。オフライン専用（サーバー/テストに影響なし）。
- **COM AI常時強化（2026-07-11）**：難易度選択は設けず、COMは常に強い設定とする。`aiPickTop` は最善評価の90%以上・最大3候補に限定し、弱い候補を展開の多様性だけを理由に選ばない（同等の良手間では小さな乱数を残す）。`aiFollowUpAttackValue` でパス/ドリブル後の次ターンに現在地または1回の移動から作れるシュート期待を先読みし、単なる横パスより連続攻撃を優先する。`aiAttackUrgency` で赤(COM)の得点差・後半進行度を評価し、負けている終盤は攻撃性を上げ、リード中は無謀な突撃を抑える。守備は青保持者がシュート圏ならレーン封鎖3体、ゴールまで5マス以内なら2体へ増員。既存のワンパターン回避、自陣停滞対策、SS1トップ常時マークは維持する。`tests/ai-behavior.test.mjs` に1ターン先読みと終盤戦術の回帰を追加し、`npm test` 74件・i18n監査候補0件・Worker型チェックが成功済み。
- **通常移動の一斉再生（2026-07-10）**：リプレイ中、**連続する通常移動（'move'／ボール非運搬の`piece.moved`）はグループ化して`Promise.all`で同時にアニメ再生**する（ローカル=`execPlainMovesTogether`／オンライン=`replayServerPieceMovesTogether`+`isPlainServerMove`）。通常移動は実行時に相互作用（タックル/カットイン/ボール処理）を起こさないため解決順・結果は順次実行と同一。パス/ドリブル/シュート等は従来どおり順次。「1駒ずつ動くのは遅く、一斉の方が気持ちいい」というユーザー要望。1件ずつのawaitに戻さないこと。
- **ローカルリプレイの駒移動アニメ（2026-07-10）**：ローカル（AI戦）の `execCommand` の move/dribble と `handleOffside` の位置戻しは、駒をワープさせずオンライン再生（`replayServerPieceMove`/`replayServerOffsideReset`）と同じ滑走アニメ（`animatePieceMove`＋`replayHiddenPieceIds`で本体を一時非表示→完了後に座標確定）で動かす。ドリブルは駒とボールを並走（`Promise.all`）。オフサイド後のキッカーへのボール受け渡しも `animateBallFlight` で飛ばす。「駒がワープしてボールだけ滑らかに動くのは変」というユーザー指摘によるもので、座標の直接代入＋render()だけの移動に戻さないこと。
- **ターンエンド/ターンスタート帯（2026-07-10実装）**：Unity `BattleSequence.TurnElement` 準拠で、入力終了（決定/時間切れ→`resolveSimultaneousTurn`冒頭・オンラインは`playServerResolution`冒頭）に「ターンエンド」、次ターンの入力開始時に「ターンスタート」の帯カットインを表示する。素材はUnity `cutin/turn_cutin.png`（紫グラデ帯・`CUTIN.turnEnd/turnStart`共用）＋`.cutin.turnband`で中央テキスト。文言はUnity LocalizeMaster正式値（`btl_cti_turnstart/turnend`＝ターンスタート/ターンエンド）。旧・無地の`start_turn`区切り帯は置き換え済み。
- **カットインのプレートサイズ統一（2026-07-10）**：カットインは全種とも画面中央の同一サイズ帯（`.cutin`、最大520px・`cutinIn`スライド）で表示する（Unity `BattleTurnCutInBehaviour` 準拠。元画像も全種340×147で統一されている）。旧・駒追従の小型プレート（`.cutin.onpiece` 120px・`cutinPop`）は「プレートの大きさが揃っていない」というユーザー指摘で廃止済み。`showCutinAtPiece(key,dur,x,y)` は互換のため残置し座標は無視して中央帯に出す（呼び出し38箇所の座標引数は発生地点の記録として残す）。小型プレートを戻さないこと。
- **ハーフタイム幕間（2026-07-10実装）**：`playHalftimeCurtain()`＝暗転（`#halftimeDim`）＋HALF TIME帯（1.4s）＋前半スコア表示（0.7s）→明け→キックオフ帯。オフライン`halftime()`とオンライン`halftime`イベント再生の両方で共通使用（Unity `ShowHalfTimeStart/End`の暗転に相当）。
- **対戦中のランクバッジ（2026-07-10実装）**：盤上の駒に編成画面と同じ★アイコン（`--cost10〜30`）の`.pieceRank`バッジを表示（Unity版のCost表示に相当。右下・駒幅62%）。
- **ゴール演出（第4弾。2026-07-10実装。同日増強）**：自分の得点は追加で**win_efect回転バースト（`#goalBurst`）＋チーム色×金の紙吹雪26個（`#goalConfetti`）＋シェイク3px/0.3s**。失点側は従来どおり地味なまま（祝わない原則）。全ゴール（通常/PK/FK/CK経由）はオフライン=`scoreGoal`／オンライン=`shot.goal`再生分岐から `playGoalCelebration(team)` を経由する（合計約2秒・秒数は`GOAL_FX`に集約）。4段構成＝①一拍0.3s（**自分の得点のみ**画面シェイク`body.goalShake`）→②GOALカットイン1.0s＋フラッシュ（自分=白強`#goalFlash.mine`/失点=赤弱`.concede`。失点はカットイン彩度落とし`.cutin.concede`＋SE無音）→③スコアパンチ（並行。`#scoreSelf/#scoreOpp`の`.punch`バウンス＋三角錐グロー金/赤）→④収束0.7s（自分=チーム色拡散光`#goalConverge`/失点=暗転`#goalConcedeFade`。**失点は祝わない**）。タップで④スキップ、`goalFxActive`で二重発火ガード。`showCutin`は`opts.cssClass`/`opts.quietSe`対応。ロジック（得点判定/スコア/キックオフ/同期）は不変で、オンラインの3分期限はintent送信起点・オフライン20秒タイマーはinput開始起点のため演出挿入による同期ズレなし。失点用カットイン素材はUnityに無いため同一`goal.png`＋トーン差で差別化。
- **試合終了リザルト演出（第3弾。2026-07-10実装。旧`#fulltimeResult`ダイアログを置換）**：4拍構成＝①FULL TIMEカットイン（既存`#cutinLayer`のテキスト帯＋終了笛whistle）→②勝敗ワード（勝ち=金グラデWINスラム＋Unity `win_efect.png` 回転光[`--winEffect`]＋goalファンファーレSE／負け=沈むLOSE／引き分け=静かなDRAW。オンラインは自分視点）→③振り返り（三角錐スコア＋**既存ログ解析による得点タイムライン** `collectGoalTimelineFromLog()`。復元数が総得点と一致した時のみ表示）→④再戦/HOME。秒数は `RESULT_TIMING` に集約。拍2以降タップで拍4へスキップ（`.revealed`）。二重発火ガード `resultSequenceActive`。呼び出しはオフライン=`resolveSimultaneousTurn` のmatchOver分岐／オンライン=`playServerResolution` 末尾（`cutinAlreadyShown:true`、観戦者は対象外。投了等の異常終了系は従来のまま）。再戦=オフライン`resetBtn.click()`／オンライン`requestOnlineRematch()`（既存ハンドシェイク。`match.rematch.requested`受信で`online.opponentWantsRematch`を立て「相手が再戦を希望しています」注記、`match.rematch.started`でオーバーレイ自動クローズ）。**これが無いと試合終了後に再開手段が見えず手詰まりになる**。文言はL10N（`rsWin/rsLose/rsDraw/rsRematch/rsHome/rsGoal/rsOppWants/rsRematchSent`）。専用画像が無いカットインは `.cutin.textband` でテキスト帯表示。
- **フッター/ヘッダーの重なり順（2026-07-09修正・戻さないこと）**：`#board`/`#selectFan` のゴール行は盤画像の上下へ透明のままはみ出すが、`.boardWrap` が positioned のため素のままでは透明グリッドが後続の `.footerBar` より手前になり **TURN END がタップ不能になる**（スマホで発覚した実バグ）。対策として `.wrap>.hud/.onlineBar/.menuBar/.footerBar` に `position:relative; z-index:20` を付与済み（`#selectFan` の z-index:30 より下なので弧ポップアップは従来どおり最前面）。ゴールマスの `pointer-events:none` だけでは不十分（親の `#board` 自体がヒットテストに掛かる）。
- **多言語化（2026-07-10）**：`football-chess-prototype.html` 内の `L10N` / `t()` がUI文言の入口。対応ロケールは **ja / en / ko / es / pt / de / zh-CN**。`fc.locale`（localStorage）→ブラウザ言語→ja の順で初期言語を決定し、設定画面の `#ogSetLanguage`（プルダウン）で即時切替・永続化する。ゲーム内部の `state.half` は日本語値を判定用に保持し、表示時だけ翻訳すること。機械訳は公開後に各言語のネイティブレビューを行う。UI背景に文字を焼き込まない方針で、キックオフ帯は生成した文字なし `assets/kickoff-l10n-banner.png` にDOMラベルを重ねる。
- **L10N起動ガード（2026-07-10修正）**：全ロケールへ後付けキーを追加する際、未作成の入れ子辞書へ直接 `Object.assign(dict.action, ...)` すると起動時例外になり、`ogBoot()` へ到達せず盤面だけが露出する。必ず `Object.assign(dict.action||(dict.action={}), ...)` のように初期化してから追加する。`tests/prototype-smoke.test.mjs` でこのガードを固定している。
- **v1.0仕上げ（2026-07-10着手）**：完成条件は `docs/V1_RELEASE_PLAN.md`。～～旧・動画型「遊び方」（`TUTORIAL_SCENES`のアニメ再生＋MP4収録前提）は2026-07-11に**静的1枚ガイドへ置換済み**（プレイ動画は作らない方針・ユーザー指定。`TUTORIAL_GUIDE`＝5項目の盤面図解＋矢印＋説明文、`#tutorialSheet`に縦積み。文字は全てDOM/L10Nで7言語維持。`docs/TUTORIAL_VIDEO.md`は削除、テレメトリ`tutorial.scene_complete`も廃止）。`npm run audit:i18n` はL10N外の日本語候補を列挙する。2026-07-10第2〜15便で主要UI、通知、エラー、試合ログを辞書化し、監査候補は0件。内部判定文字列は表示前に `logBattleDetail()` で辞書キーへ変換し、監査対象から除外する。得点タイムラインは表示文言を解析せず、`halfId` / `teamId` を含む構造化ログから復元する。構造化ログは `logKey(key, params)` で辞書キーと変数を保存し、言語変更時に`relocalizeLog()`で再描画する。`tests/prototype-smoke.test.mjs` とゲームコアの長時間/両チーム同時入力テストを追加。匿名統計は `/telemetry` へ許可イベント・数値だけを送り、表示名/roomCode/認証情報/自由入力は送らずWorkers構造化ログへ出力する。残作業はja/en人手レビュー、各言語の文字切れ確認、言語依存文字を含む画像の最終確認。
- **v1.0仕上げ第1便の公開記録（2026-07-10）**：実装コミット `dfe0ae5` を `origin/codex/prototype-throughpass-move-fix` へPushし、本番 `https://mini.footballchess.io` へデプロイ済み（Worker Version `157180df-af1f-4c97-ba99-c837d02b7a4f`）。`npm test` 44件、Worker型チェック、HTML構文、Worker dry-runが成功し、デプロイ後の本番HTML SHA-256はローカル生成物と一致、healthはproduction正常応答、公開HTML内の `tutorialOverlay` と「動画で遊び方」を確認した。
- **多言語化第15便の公開記録（2026-07-10）**：実装コミット `cbffe4d` を `origin/codex/prototype-throughpass-move-fix` へPushし、本番 `https://mini.footballchess.io` へデプロイ済み（Worker Version `bd6edda9-71a6-4bd0-90d3-fa80a8be50e4`）。`npm run audit:i18n` は候補0件、`npm test` 46件、Worker型チェック、Worker dry-runが成功。デプロイ後の本番HTML SHA-256 `6c82aa4d194b6e542d26b3a99cf6a5802c154b8811128b62f395f512d58d17b9` はローカル成果物と一致し、`/api/universofutbol/football-chess/health` はproduction正常応答を確認した。
- **ホーム起動不能修正の公開記録（2026-07-10）**：修正コミット `0631647` を `origin/codex/prototype-throughpass-move-fix` へPushし、本番 `https://mini.footballchess.io` へデプロイ済み（Worker Version `6391d515-853d-4eda-8579-f49ded169204`）。`npm run audit:i18n` は候補0件、`npm test` 47件、Worker型チェック、Worker dry-runが成功。本番ブラウザでホームメニュー表示→KICK OFF→32マス盤面と操作可能なTURN ENDを確認した。本番HTML SHA-256 `5bb265dfa455fbe6df551c89f7c63df79ca8de81d30c157ffc14ae144ce13760` はローカル成果物と一致し、healthはproduction正常応答。
- **Playwright E2E実行記録（2026-07-10）**：`npm run test:e2e` の全10件を成功確認。起動例外監視、単一「対戦」導線、COM開始、初回チュートリアル案内、15秒COMフォールバック、キャンセル、COM戦フルタイム完走（2.2分）、実WebSocketの部屋作成/2席参加/両席入力/ターン解決/再接続/投了/再戦/退出、自動マッチングの青赤席割り当てを検証した。長時間ケースは `tests/e2e/com-match.spec.mjs`、実通信は `tests/e2e/online.spec.mjs`。実機Safari/Chrome、低速回線・スリープ復帰は別途必要。
- **COM AI改善の公開記録（2026-07-11）**：実装コミット `dfd8e68` を `origin/codex/prototype-throughpass-move-fix` へPushし、本番 `https://mini.footballchess.io` へデプロイ済み（Worker Version `8789912b-7a34-48c5-a198-ea1a35c12282`）。良手候補からの重み付き抽選で行動のワンパターン化を抑え、相手位置・駒ランクを考慮するシュートレーン封鎖、受け手マーク、帰陣、攻撃時の被タックルリスク評価を追加した。`npm test` 60件、`npm run audit:i18n` 候補0件、Worker型チェック、Worker dry-runが成功。本番HTML SHA-256 `090f25da217cdb6d51043c8492ac065c545c73f7bd6c369735fcc87f43407870` はローカル生成物と一致し、healthはproduction正常応答を確認した。
- **クラウドClaude未反映4コミットの公開記録（2026-07-11）**：手動保存パッチの整合性を確認後、①オンライン対戦のCOMフォールバック完全秘匿化 `ed2d492`、②プレミアム限定ランク変更ヒント＋TEAM COSTの7言語化 `d915992`、③ヘッダー実名/COMの6フォーメーション別ペルソナ＋プレイヤー名永続化修正 `80e8dab`、④シュートの鋭いライナー弾道 `9292cf8` を `git am -3` で競合なく取り込み、`origin/codex/prototype-throughpass-move-fix` へPushして本番 `https://mini.footballchess.io` へデプロイ済み（Worker Version `684cec14-75b8-44c7-88d3-574d602c82c3`）。`npm test` 63件、`npm run audit:i18n` 候補0件、Worker型チェック、Worker dry-runが成功。本番HTML SHA-256 `ac9998f45eb8e7bc3f2817ef4cb92aaed7c7c793e1aa9521a755389dd5f260d1` はローカル生成物と一致し、healthはproduction正常応答を確認した。
- **ログイン状態の一貫性修正（2026-07-11）**：`?room=` 付き共有対戦URLでも `#uf_sso` / `?sso=` を処理してからセッションを復元する。セッション復元の通信失敗時は、保存トークンを次回再試行用に残しつつ実行中の `ogAuth` を空にし、「ゲスト表示なのに古いBearerだけ送る」状態を作らない。ログアウトAPIの失敗時はローカルセッションを消さず、利用者へ再試行を案内する。設定のアカウント欄では「アカウント名」と端末保存の「対戦名」を分けて表示する。ポータルの正しいログイン起点は `https://universo-futbol.com/api/auth/launch`（ハイフンあり）。これらは `tests/prototype-smoke.test.mjs` の認証回帰テストで固定し、`npm test` 72件・i18n監査候補0件・Worker型チェック・dry-runが成功済み。

## 検証方法
- **構文チェック**：`<script>`〜`</script>` を抽出して `node --check`。
  ```bash
  S=$(grep -n "<script>" football-chess-prototype.html | tail -1 | cut -d: -f1)
  E=$(grep -n "</script>" football-chess-prototype.html | tail -1 | cut -d: -f1)
  awk -v s="$S" -v e="$E" 'NR>s && NR<e' football-chess-prototype.html > /tmp/fc.js
  node --check /tmp/fc.js
  ```
- **ロジック検証**：DOMをスタブ化して `eval` し、対象関数を実行時テスト（このリポジトリでの標準手法）。
- **Worker型チェック**：Cloudflare側を触ったら `npm run check:worker` を必ず実行する。
- **ゲームコア回帰テスト**：`npm test`（`tsc -p tsconfig.test.json` で `.test-dist` にビルドし、Node標準 `node --test` で `tests/game-core.test.mjs` を実行）。
- **Worker dry-run**：公開前やDurable Object変更後は `npx wrangler deploy --dry-run` でバンドル確認する。
- **ブラウザE2E**：`npm run test:e2e` はPlaywright Chromiumと `wrangler dev` を使い、ローカルHTML、Durable Object、実WebSocketを検証する。`playwright.config.mjs` はポート8787・スマホ相当390×844・直列実行。COMフル試合は最大10分、オンラインは最大5分かかるため、短い起動テストと長時間ケースを分けて実行してよい。見た目の最終確認とiPhone Safari / Android Chromeの実機確認は別途行う。

## 編集上の注意
- 画像base64を含む巨大な行があるため、`Read` は範囲指定で。`cat`/`sed` での全文出力は避ける。
- 内部ロジック（移動可否/パス範囲/確率/コマンド確定）の変更は慎重に。UI改修時は「表示のみ変更、ロジック不変」を原則とする。
- Codexで継続する場合は、実装・検証・ローカルURL確認までこの単一HTML版で完結させる。演出移植はUnity C#を調べ、ボール軌跡・駒移動・カットインなど小さい単位で移す。
- Git運用：remote は `origin https://github.com/GadeDev/football-chess-next.git`。最新作業は `origin/codex/prototype-throughpass-move-fix` にあり、ローカルにも同名の追跡ブランチを作成済み（2026-07-06。以前はローカル `master` が同リモートブランチを追跡していた）。`origin/main` は別履歴の古い系統なので、統合/上書きはユーザー確認なしに行わない。Pushはユーザーから明示依頼があった場合のみ行う。
- デプロイ：Worker公開は `npm run check:worker` → `npx wrangler deploy --dry-run` → `npm run deploy:worker`（= `build:assets` + `wrangler deploy`）の順で行う。**⚠ デプロイは必ずこのフォルダ（football-chess-repo）の最新HEADから行うこと**。2026-07-10に別チェックアウト（古い作業コピー）からのデプロイで本番が旧版に巻き戻る事故が発生（どのコミットとも一致しない内容が配信され「実装したはずの機能が見えない」報告になった）。複数セッション/Codex並行作業時は、デプロイ直前に `git log --oneline -3` で最新確認し、デプロイ後は本番ハッシュ照合（`curl -sL https://mini.footballchess.io/ | shasum -a 256` がローカル `public/index.html` と一致）まで行う。本番が古いと疑われたら `npx wrangler deployments list | tail` で自分以外のデプロイが無いか確認する。**正式公開URLは `https://mini.footballchess.io`**（Workerカスタムドメイン、2026-07-08設定。`universofutbol-football-chess` Workerに紐付け。workers.dev URLも併存）。デプロイ後は `curl -sL https://mini.footballchess.io/ | shasum -a 256` がローカル `public/index.html` と一致すること、`/api/universofutbol/football-chess/health` が `{"ok":true,...,"environment":"production"}` を返すことを確認。**旧Worker `football-chess-next`（旧Viteビルド配信、親フォルダ側）はデプロイ禁止・廃止予定**。`mini.football.io`（HostGator向きの別ドメイン）は本プロジェクトと無関係。ENVIRONMENTは2026-07-06からproduction。ローカル`wrangler dev`は`.dev.vars`（Git管理外）でdevelopment表示。
- HTML配信：公開URLの `/` はゲームHTML本体を static assets で配信する（2026-07-06実装）。ソースはリポジトリ直下の `football-chess-prototype.html` が唯一の正で、`npm run build:assets` が `public/index.html` へコピーする（`public/` はGit管理外の生成物。直接編集しない）。同一オリジン配信なのでHTML内のオンラインAPIは自動で本番Workerへ向く。ROOM共有URLは `https://universofutbol-football-chess.yanagiho.workers.dev/?room=FC-XXXX-XXXX` 形式。
